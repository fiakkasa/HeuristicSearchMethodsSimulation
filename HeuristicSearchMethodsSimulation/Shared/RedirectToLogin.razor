@inject NavigationManager NavigationManager

@code{
    protected override async Task OnInitializedAsync()
    {
        NavigationManager.NavigateTo(
            await Task.FromResult($"{NavigationManager.BaseUri}Identity/Account/Login"),
            forceLoad: true
        );
    }
}
