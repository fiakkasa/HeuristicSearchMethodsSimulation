@inject NavigationManager nav

@code{
    protected override async Task OnInitializedAsync()
    {
        nav.NavigateTo(
            await Task.FromResult($"{nav.BaseUri}Identity/Account/Login"),
            forceLoad: true
        );
    }
}
